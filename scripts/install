#!/usr/bin/env bash

pushd /mnt/home/ezequiel/.dotfiles || exit

nix-shell -p nixUnstable git --run "
    git add . &&
    rm flake.lock &&
    nixos-install --impure --root /mnt --flake .#ramis
"

popd || exit